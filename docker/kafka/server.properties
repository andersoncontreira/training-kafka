# Kafka Server Properties - KRaft Mode (Kafka 4.x)

# Unique identifier for the broker
node.id=1

# KRaft mode: this node acts as both broker and controller
process.roles=broker,controller

# Quorum voters: nodeId@host:controllerPort
controller.quorum.voters=1@kafka:9093

# Listener configuration
listeners=PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
advertised.listeners=PLAINTEXT://kafka:9092
inter.broker.listener.name=PLAINTEXT

# Required in Kafka 4.x KRaft mode
controller.listener.names=CONTROLLER
listener.security.protocol.map=PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT

# Log storage
log.dirs=/var/kafka-logs

# Network and I/O threads
num.network.threads=3
num.io.threads=8
socket.send.buffer.bytes=102400
socket.receive.buffer.bytes=102400
socket.request.max.bytes=104857600

# Log retention
log.segment.bytes=1073741824
log.retention.hours=168
log.retention.check.interval.ms=300000

# Replication factor of 1 for single-node setup
offsets.topic.replication.factor=1
transaction.state.log.replication.factor=1
transaction.state.log.min.isr=1

# Faster startup for development
group.initial.rebalance.delay.ms=0
